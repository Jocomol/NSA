CREATE TABLE Messwerte(
        ID INTEGER PRIMARY KEY AUTOINCREMENT,
        OfferedIP TEXT,
        ServerIdentifier TEXT,
        Latency TEXT,
        Netmask TEXT,
        DNS TEXT,
        Router TEXT,
        Domainname TEXT);
